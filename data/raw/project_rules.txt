RAG 学习实验要求

每次实验只改变一个主要因素，并记录资料版本、分块配置、向量模型、检索数量、结果和失败案例。
API 密钥只能通过环境变量提供，不能写进源代码、笔记或实验报告。
回答中的引用必须对应实际提供给生成器的片段，并能够定位到原始文件和行号。
日志应使用一次问答的追踪编号关联检索和生成步骤。模型超时可以有限重试，但不能无限重试。
资料、分块配置或向量模型发生变化后，相关索引和缓存必须失效或重新构建。
